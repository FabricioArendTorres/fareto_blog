services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    
    ports:
      - "8083:80"  # make docker only accessible internally and from port 8083
      
    labels:
      - "coolify.domain=fareto.dev"
      - "coolify.port=8083"